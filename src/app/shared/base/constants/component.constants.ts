import {
  ComponentAppearance,
  ComponentColor,
  ComponentShape,
  ComponentSize,
} from '../types';

export const DEFAULT_SIZE: ComponentSize = 'md';

export const DEFAULT_COLOR: ComponentColor = 'primary';

export const DEFAULT_APPEARANCE: ComponentAppearance = 'filled';

export const DEFAULT_SHAPE: ComponentShape = 'rounded';

/**
 * Semantic color palette for components.
 *
 * Maps to your design tokens (CSS variables/theme).
 *
 * Example:
 * primary   -> --erp-primary
 * secondary -> --erp-secondary
 * success   -> --erp-success
 */
export type ComponentColor =
  | 'primary'
  | 'secondary'
  | 'tertiary'
  | 'success'
  | 'warning'
  | 'danger'
  | 'info'
  | 'neutral'
  | 'inherit';
